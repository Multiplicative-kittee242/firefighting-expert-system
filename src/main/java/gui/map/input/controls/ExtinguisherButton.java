package gui.map.input.controls;

import gui.map.input.ExtinguisherButtonGroup;

/**
 * Toggle button for marking a single portable extinguisher used. Not yet placed on the map —
 * {@link ExtinguisherButtonGroup} is currently constructed with no coordinates, so no
 * instance of this button is ever created; the glyph is left at the inherited blank default
 * until placement is introduced.
 */
public class ExtinguisherButton extends AbstractToggleButton {
}
